import ProfileContainer from '@/src/modules/profile/container/ProfileContainer';

export const metadata = {
  title: 'Perfil · AURASTATION',
  description: 'Edita tus credenciales y tu foto de perfil.',
};

export default function ProfilePage() {
  return <ProfileContainer />;
}
