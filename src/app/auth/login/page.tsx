import LoginContainer from "@/src/modules/auth/login/container/LoginContainer";

export const metadata = { title: 'Iniciar sesión · AURASTATION' };

export default function LoginPage() {
  return <LoginContainer />;
}